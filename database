default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  username: postgres
  password: # Make sure this matches your PostgreSQL password
  host: localhost

development:
  <<: *default
  database: autodialer_development
